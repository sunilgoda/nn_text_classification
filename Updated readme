 README.md 
@@ -1,2 +1,2 @@
# nn_text_classification.
Text Classification done to identify the correct category of HealthLine Services support ticket using CNN, RNN, LSTM, GRU.
Text Classification done to identify the correct category of Services SUPPORT TICKET SYSTEM using CNN, RNN, LSTM, GRU.
Text Classification done to identify the correct category of HealthLine Services support ticket using CNN, RNN, LSTM, GRU.Text Classification done to identify the correct category of Services SUPPORT TICKET SYSTEM using CNN, RNN, LSTM, GRU
T
